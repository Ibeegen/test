# MOC Atelier Storefront

Website ban hang giao dien moi cho MOC Atelier, duoc xay dung bang React + Vite.

## Chay local

1. Cai dependencies:
   `npm install`
2. Chay server phat trien:
   `npm run dev`
3. Build ban production:
   `npm run build`
